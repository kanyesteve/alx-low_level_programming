#include <stdio.h>

/**
 * main-Serves as the entry for the program
 *
 * Return: by default zero f no error otherwise retturns non-zero value
 */
int main(void)
{
	puts("\"Programming is like bulding a multilingual puzzle");
	return (0);
}
